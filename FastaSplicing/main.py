import union_input
import structure_func

def Main():
    # union_input.UnZipping()
    # UnZipping function takes a folder "zips" with zip archives and extracts them into this folder
    # union_input.Union()
    # Union function takes a "zips" folder with extracting archives and creates new directories in "input_files" with unions of files 
    structure_func.StructureQualifier()
    # StructureQualifier function takes input fasta-files and structurized them into a new directories in "structured_files" by segments of flu
Main()